# `.claude/` in this repository

- **`skills/`** holds Agent Skills. Each workflow is a directory containing `SKILL.md` (instructions) and usually scripts, JSON, or other assets.

There is no separate commands layer. Load `SKILL.md` from here when running workflows in Cursor or Claude Code. See the skills table in [../CLAUDE.md](../CLAUDE.md).
